<adonisControls:AdonisWindow x:Class="FModel.Views.MissingAssetExtractor"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
                             xmlns:converters="clr-namespace:FModel.Views.Resources.Converters"
                             Title="Missing Asset Extractor" WindowStartupLocation="CenterScreen"
                             Height="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenHeight}, Converter={converters:RatioConverter}, ConverterParameter='0.55'}"
                             Width="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenWidth}, Converter={converters:RatioConverter}, ConverterParameter='0.45'}">
    <adonisControls:AdonisWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Resources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </adonisControls:AdonisWindow.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Paste asset property JSON paths (one per line) or log entries that include 'Missing asset:' lines. FModel will resolve the underlying assets and export their properties as JSON files."
                   TextWrapping="Wrap" />
        <TextBox Grid.Row="2" Margin="0 0 0 10" TextWrapping="Wrap" AcceptsReturn="True" AcceptsTab="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" MinHeight="120"
                 Text="{Binding InputText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 0 0 10">
            <Button Content="Clear" Margin="0 0 10 0" Width="80"
                    IsEnabled="{Binding IsProcessing, Converter={x:Static converters:InvertBooleanConverter.Instance}}"
                    Click="OnClearClick" />
            <Button Content="Extract" Width="120" IsDefault="True" Click="OnExtractClick"
                    IsEnabled="{Binding IsProcessing, Converter={x:Static converters:InvertBooleanConverter.Instance}}" />
        </StackPanel>

        <GroupBox Grid.Row="4" Padding="{adonisUi:Space 0}" Header="Results" Background="Transparent">
            <DataGrid ItemsSource="{Binding Results}" Margin="0"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserReorderColumns="True"
                      CanUserResizeColumns="True"
                      CanUserSortColumns="True"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Detected Path" Binding="{Binding OriginalPath}" MinWidth="150"
                                        Width="2*" />
                    <DataGridTextColumn Header="Asset Path" Binding="{Binding AssetPath}" MinWidth="150" Width="2*" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" MinWidth="80" Width="Auto" />
                    <DataGridTextColumn Header="Details" Binding="{Binding Message}" MinWidth="180" Width="3*" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <TextBlock Grid.Row="5" Margin="0 10 0 0" Text="{Binding StatusMessage}" TextWrapping="Wrap"
                   Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}" />
    </Grid>
</adonisControls:AdonisWindow>
